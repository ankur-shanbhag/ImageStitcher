# auto-replace paths - Do not modify manually

# Hadoop configurations
hadoop.home=${hadoop.home}
hadoop.conf.path=${hadoop.conf.path}
delete.hdfs.dirs=${delete.hdfs.dirs}
error.threshold=${error.threshold}

# Hadoop file paths
hadoop.input.dir=${hadoop.input.dir}
hadoop.output.dir=${hadoop.output.dir}
hadoop.image.source.file=${hadoop.image.source.file}
hadoop.image.target.file=${hadoop.image.target.file}

# Local file paths
local.image.source.file=${local.image.source.file}
local.image.target.file=${local.image.target.file}

base.project.path=${project.build.directory}

# gnuplot configurations
gnuplot.process.path=${gnuplot.process.path}
gnuplot.output.path=${gnuplot.output.path}